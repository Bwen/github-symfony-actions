on:
  push:
    branches:
      - 'master'
    tags:
      - 'v*'

jobs:
  publish-version:
    runs-on: ubuntu-22.04
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_LOGIN }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: ./
          file: ./Dockerfile
          builder: ${{ steps.buildx.outputs.name }}
          push: true
          tags: ${{ secrets.DOCKERHUB_LOGIN }}/symfony-gh-actions:latest,${{ secrets.DOCKERHUB_LOGIN }}/symfony-gh-actions:${{ github.ref_name}}
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_LOGIN }}/symfony-gh-actions:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKERHUB_PASSWORD }}/symfony-gh-actions:buildcache,mode=max
